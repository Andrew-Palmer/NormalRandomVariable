# NormalRandomVariable Usage Example

First, ensure that the library has been installed (see [instructions](../README.md)). Then, build and run:


    mkdir build
    cd build
    cmake ..
    make
    ./example